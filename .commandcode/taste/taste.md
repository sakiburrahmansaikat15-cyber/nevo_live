- Prefers detailed, thorough upfront documentation and specs before implementation — the user shared an extremely comprehensive plan document covering all schemas, routes, entities, and task lists before coding began; later UI tasks also arrive as exhaustive specs (goals, hard rules, layout zones, exact color palette hexes, typography, animation list, component folder structure, code rules, and required output format). Confidence: 0.95
- Prefers monorepo architecture with npm workspaces for multi-portal projects. Confidence: 0.9
- Prefers separate React apps per portal (rather than a single app with role-based routing) for cleaner separation and independent deploys — specs consistently describe distinct portals (user H5 app, admin console, agency, coin/agent portal). Confidence: 0.95
- Prefers TypeScript throughout the entire stack (both frontend and backend). Confidence: 0.9
- Prefers TailwindCSS for styling React applications. Confidence: 0.9
- Prefers Zustand + React Query (TanStack Query) for state management over Redux Toolkit or plain Context. Confidence: 0.9
- Prefers Zod for request validation and type-safe schemas shared between frontend and backend. Confidence: 0.9
- Prefers responsive web-only approach (no React Native) — single web codebase that works on mobile browsers. Confidence: 0.9
- Prefers using real credentials/configuration over mock or dev fallback implementations when real credentials are available — expects production-quality integrations even during development. Confidence: 0.8
- Develops on Windows — shell commands, path resolution, and environment variable handling (e.g. newline escaping in .env files) must account for PowerShell/cmd and Windows filesystem conventions. Confidence: 0.9
- Prefers hierarchical consistency — when a flow pattern exists at one level (e.g., user→agent purchase), the same pattern should be mirrored at the parent level (agent→admin) with identical semantics, states, and UI patterns (spec repeats the same payment/verification flow at each role level: screenshot + TXID). Confidence: 0.95
- Prefers permanent relationship binding on first interaction — the first time an agent processes a user's order, the user is irreversibly bound to that agency; subsequent agents are blocked from serving that user. Confidence: 0.9
- Prefers comprehensive audit trails — every transaction, order, and state change must be persisted as a detailed record, and every user/agent/admin must be able to view their full history. Confidence: 0.9
- Prefers being asked clarifying questions with concrete, labeled options when design decisions are needed — appreciates being presented with clear choices (e.g., "Option A: ... Option B: ...") rather than open-ended questions; answers by picking the offered option's number alone (e.g., replying just "3") without restating the choice; in the roulette rebuild locked in the three recommended options (European wheel, fix engine, stay on React 18) without further discussion. Confidence: 0.9
- Prefers external config/credentials files (e.g., JSON) over inlining complex structured values in .env — avoids escaping issues like `\n` in private keys that plague dotenv on Windows. Confidence: 0.8
- Prefers graceful degradation for external services (Firebase, etc.) — a failing service should not crash the server; the app should degrade gracefully and return meaningful error responses (e.g., 503 with an explanation) rather than crashing or surfacing stack traces. Confidence: 0.7
- Shares raw terminal output and full logs when reporting issues — pastes complete console output including errors, timestamps, and request logs rather than summarizing symptoms (again pasted an enormous raw server log of repeated `E11000 duplicate key` errors with zero accompanying text, leaving diagnosis and fixing entirely to the assistant). Confidence: 0.95
- Communicates both bug reports and feature requests tersely in a single line — quoting exact UI copy or stating the expected behavior (e.g., "There is no admin endpoint on admin panel to receive the complains/reports", "In Me Tab Put The Contact Us And Let The User Send Messages Towards The Admin Directly", "why this Host unavailable — cannot send gifts right now, any one can send gift if they have balance") — leaves root-cause investigation and full implementation to the assistant rather than providing hypotheses, logs, or design specs; sometimes reports by pasting the raw error alone with zero accompanying text (e.g., a bare browser console log paste with no question or expected outcome — again pasted a bare MIME-type "blocked because of a disallowed MIME type" console error with no explanation); also selects among offered options with a bare number ("3"). Confidence: 0.99
- Prefers structured, fill-in-the-blank checklists when the assistant needs to gather configuration/credentials — wants to be explicitly asked "Firebase APP ID: , ETC: ETC:" rather than the assistant assuming or inferring values. Confidence: 0.8
- Prefers to personally provide configuration values and credentials rather than having the assistant discover or set them — wants to be prompted for each required item explicitly. Confidence: 0.7
- Prefers communicating in Bengali for technical bug reports and feature requests — switches to Bengali naturally when describing issues (e.g., the entire count-reward tier spec was written in Bengali with lakh/crore denominations), and live-room bugs reported as "Live থেকে Host এর Profile এ ঢোকা যায় না"), though also comfortable with English. Confidence: 0.92
- Provides diagnostic hypotheses and context about what has already been attempted when reporting bugs — doesn't just paste the error, but also shares their own theory about the root cause (e.g., "I think the JWT token has an issue, I've already set isAdmin: true"). Confidence: 0.8
- Expects cross-portal consistency — when a fix is applied to the admin portal, the same fix should be applied to the user portal (or vice versa) so both sides behave identically; phrases requests to cover every frontend app at once ("add .htaccess to make all the routers work flawless"; the Mic-icon official-notification request explicitly scoped "App + Website" in a single sentence). Confidence: 0.8
- Deploys the SPA frontends on Apache hosting that supports `.htaccess` with `mod_rewrite` (cPanel-style, `AllowOverride All`) — requests Apache-specific SPA fallback config (rewrite every non-file route to `index.html` so refreshes and deep links never 404, plus gzip and asset caching) placed in each app's `public/` so Vite copies it verbatim into `dist/`; expects the same file in every frontend app, not just one. Confidence: 0.7
- Prefers admin-driven user provisioning — admins should be able to create role-specific accounts (e.g., agents) directly from the admin panel by filling in credentials and profile details, rather than requiring those users to self-register. Confidence: 0.7
- Prefers automatic related-record creation when provisioning role-typed users — e.g., when creating an agent user, the system should automatically create the associated Agency record with sensible defaults in the same operation. Confidence: 0.6
- Prefers graceful deprecation — when removing a role/system, wants its features preserved and transferred to existing portals first before deletion, rather than losing functionality. Confidence: 0.8
- Prefers a single `npm run dev` command that starts all dev servers (backend + all frontends) simultaneously rather than running each separately. Confidence: 0.7
- Expects conventional npm scripts to work at the monorepo root — runs plain `npm run build` (not just scoped scripts like `build:all`) and reports when the root script is missing, so root package.json should expose standard aliases (`build`, `dev`) in addition to per-package scripts. Confidence: 0.6
- Prefers lightweight, minimally-invasive feature implementations over full system restorations — when offered a choice between a "light" code-join-only version (features consolidated in existing apps, no separate portal) and a full restoration of a previously-removed portal, chose the light option. Confidence: 0.5
- Expects accurate, state-specific user-facing messages — when a search yields no results the UI should clearly say "not found" with a helpful next step (e.g., re-check the UID), not surface a misleading "invalid" error; the system should distinguish "not found" from "invalid" in its copy and errors (including correct HTTP status semantics — e.g., invalid input should be 400, not a 404 that looks like a missing route). Confidence: 0.6
- Prefers displaying stable unique identifiers (UID/Agent ID) alongside display names (nicknames) in portal UIs — requested that the Agent ID be shown wherever the agent name appears so users can see and reference/link accounts; reported the missing host UID on live-stream cards ("Tab এ আলাদা ID দেখাবে না") as a critical bug. Confidence: 0.65
- When reporting a failing flow, asks for detailed diagnostic logging to be added (both frontend console and backend server logs) — wants status codes, response bodies, and exact failure reasons captured so the root cause is identifiable, rather than just a generic error message. Confidence: 0.7
- Expects data-integrity to be self-healing, not just reference validation — stale/corrupted state (a stored reference like agencyId pointing to a deleted, banned, or self-owned record, or a user's `role` field wrongly overwritten to 'host' instead of 'agent') should be auto-corrected at entry points so valid actions (linking to an Agency, agent login/search) succeed instead of being blocked; e.g., approved a login-time guard (`ensureAgentRole`) applied across every auth path (password, OTP, Google, dev) that derives missing roles from legacy flags and auto-corrects role/agencyId. Confidence: 0.7
- Expects the frontend to be null-safe against orphaned backend references — a populated field (`hostId`) can legitimately be `null` when the referenced user no longer exists (Mongoose populate returns null for orphaned refs), so every consumer of such fields (isHost guard, host card, gift handler) must treat null/string/missing as "no host" and degrade gracefully instead of crashing on `null._id`; type unions should reflect the null case (`UserPublic | string | null`) and fixes should be applied to every touchpoint, not just the reported crash site. Confidence: 0.7
- Expects production-ready, deployment-quality code with explicit security measures — JWT auth with role-based access control, encrypted passwords, rate limiting, CSRF/XSS protection, input & image/file validation, secure uploads, and centralized error handling are demanded as non-negotiable in specs. Confidence: 0.8
- Expects real-time (WebSocket/socket.io) functionality — e.g., live notifications pushed to users on state changes — rather than refresh-based or polling UX. Confidence: 0.7
- Expects the assistant to operate as a full-stack architect (database design, backend, frontend, DevOps, security) delivering end-to-end implementations of complete platforms, not piecemeal changes — when requesting multi-feature work the user explicitly delegates scope ("Do whatever needs to be done at this project", "Plan And Implement Everything") rather than micromanaging; e.g., after adding a game package, expects it to be discovered, wired into the platform, and implemented end-to-end ("Game needs to be implements"); the roulette spec was handed over with "Please Alter The Roulette With This Plan" and a full production build was expected, no questions about scope. Confidence: 0.9
- Expects analytics/reporting with CSV/PDF export and charts (daily/weekly/monthly) to be standard scope for admin dashboards, alongside search, filtering, and pagination. Confidence: 0.6
- Prefers lightweight, minimally-invasive feature implementations over full system restorations — when offered a choice between a "light" code-join-only version (features consolidated in existing apps, no separate portal) and a full restoration of a previously-removed portal, chose the light option. Confidence: 0.5
- Expects fundamental business-logic updates to be applied end-to-end with matching calculations across the backend and every affected frontend — e.g., the recharge-system "fundamental" update (dedicated rechargeRate for units, bonusRate bonus, agentProfitPercent commission) touched payment.service.ts plus the user app's Recharge page (bonus breakdown shown in "You will receive") and the agent portal's Wallet (Commission Earned card), so displayed numbers always match server-side math. Confidence: 0.55
- Prefers profile stats (level, follower count, following count) displayed prominently at the top of profile pages — explicitly requested "Show Profile Level Following and follower at the top of the profile" for both the user's own profile and other users' public profiles. Confidence: 0.7
- Treats non-regression as a hard requirement when adding new features — explicitly instructed "keep anything else that are functioning"; new systems should be additive without disturbing existing flows (e.g., social/reporting features added while recharge, withdraw, agency, and notifications kept working). Confidence: 0.85
- Scopes UI redesigns strictly to presentation — explicitly forbids touching backend logic, API endpoints, WebRTC/Mediasoup, Socket.IO events, or auth; expects UI-only changes to preserve 100% of existing business logic and keep integration contracts (e.g., the `#agora-video-area` DOM id the RTC hook renders into) byte-identical so every existing feature keeps working. Confidence: 0.8
- When rebranding an app name, expects UI-only scope applied "everywhere" — update every user-visible occurrence (page titles, sidebar/header branding) across all portals, while explicitly forbidding edits to project JSON/config/package files even though they still reference the old name internally ("Dont Touch Any Project Json Files, Or Configurations, Just Update The App Name From Bogolive To Navo Live, Everywhere In The UI"). Confidence: 0.8
- Expects social/community features as standard platform scope, modeled after TikTok's interaction patterns — profile discovery/search to find each other, follow/followers with counts, and real-time user-to-user direct messaging with unread badges. Confidence: 0.6
- Expects moderation/reporting capability available to every role (users and agents) for every content/transaction entity in the platform — with admin panel endpoints to list, view, and act on reports (review/dismiss/action) and notif- Expects moderation/reporting capability available to every role (users and agents) for every content/transaction entity in the platform — with admin panel endpoints to list, view, and act on reports (review/dismiss/action) and notify the reporter of the outcome. Confidence: 0.6
y the reporter of the outcome. Confidence: 0.6
- Expects in-app direct user→admin communication (a Contact Us / support-ticket channel) as standard platform scope — users should be able to message admins directly from the app (e.g., in the Me/Profile tab), see their own message history with status (pending/resolved) and admin replies in-app, while admins manage and reply to messages from the admin panel. Confidence: 0.5
- Prefers proper SVG iconography (Font Awesome-style glyphs) over emoji characters for UI elements like currency symbols — explicitly requested "Diamond And Coin Image Fa Icon Please" to replace 💎/🪙 emoji across all portals with icon components; a shared CurrencyIcon helper (diamond=Gem/cyan, coin=Coins/yellow) per app is an acceptable implementation. Confidence: 0.6
- Prefers pure SVG for game visuals and animations — explicit hard constraint "SVG only. No Canvas. No PNG. No Lottie."; the roulette wheel is a layered SVG built from defs/gradients/filters (feGaussianBlur, feDropShadow, feSpecularLighting, masks, clipPaths) with every number as SVG path/text and no raster graphics, achieving a 3D look purely through vector gradients and filters. Confidence: 0.75
- Expects provably-fair mechanics in games that involve randomness — cryptographically secure randomness, a committed hash broadcast before the spin, the seed revealed afterward so clients can recompute the result and verify fairness; roulette rounds expose hash at round start and seed on result with client-side SHA-256 verification. Confidence: 0.7
- Expects game result reveals to be synchronized with the animation timeline — when the server emits the result the moment the spin begins (so it arrives while the wheel/ball are still animating), the client must buffer it and only reveal the winning number, win/lose sound, win badge, and particle effects once the wheel has fully stopped AND the ball has settled into the pocket ("The Result Must Not Come Before The Ball Stops Rolling"); the reveal must gate on animation-completion state (e.g., ball.settled + spinComplete), not on result arrival. Confidence: 0.7
- Expects physically-realistic game animations rather than simple monotonic easing — the roulette wheel should launch fast, decelerate, then land with a small elastic wobble into the winning pocket instead of a dead stop, and the ball should visibly roll on its own axis as it orbits (self-rotation derived from travel distance) with a motion trail that fades as it slows; "improved animation" requests imply multi-phase easing and per-object self-rotation as the baseline. Confidence: 0.65
- Expects complete deliverables with no placeholders, TODOs, or fake assets — "No placeholders. No TODO. No fake assets. Everything production-ready." applies to game builds (every component, SVG, animation, hook, store, style must be real and functional). Confidence: 0.6
- Expects accessibility as part of game/UI scope — keyboard betting controls, ARIA labels on every interactive cell, screen-reader support, and reduced-motion support are listed as requirements for games, not optional polish. Confidence: 0.6
- Defers to the existing repo stack rather than adopting versions named in a spec — the roulette spec listed React 19, react-spring, and GSAP, but the approved plan stayed on the repo's React 18.3 + framer-motion 11 with zero new dependencies and no React upgrade, treating spec dependencies as optional. Confidence: 0.6
- Expects icon/visual changes to be applied uniformly everywhere — when a currency-icon system is introduced, every remaining instance (balance cards, headers, tables, stat cards, wallet entries) across all apps must use the same icon component, with no mixing of icon sets for the same concept; will explicitly follow up with terse "everywhere" requests (e.g., "Everywhere change the icons to the previous command corresponding") to close missed spots that still use a different icon (lucide Diamond/Coins) than the established helper. Confidence: 0.6
- When the user says they added a package/app, the folder may be placed at the repository root (outside packages/) and may not yet be registered in the workspace — expects the assistant to locate the exact path (asking for it if needed) and integrate it into the monorepo (workspace registration, scripts, implementation) rather than assuming conventional placement. Confidence: 0.55
- Dictates an exact brand color palette (background, primary, secondary, accent, success, warning, danger as explicit hex codes) that all UI work must conform to — roulette spec dictated background #050816, gold accent #F5C451, success #00FF95, danger #FF4747 verbatim. Confidence: 0.65
- Expects UI work delivered as a reusable component library — small single-purpose components organized in a dedicated folder (e.g., `components/live/`) with a barrel index, shared animation variants, and clear props/callbacks wiring, rather than monolithic page JSX; game rebuilds follow the same modular pattern (game/<game>/ with engine/physics/animation/audio/store/utils/hooks/components sub-modules, barrel indexes, React.memo on cells/chips). Confidence: 0.6
- Expects the assistant to enter plan mode and ask clarifying questions (with concrete labeled options) before executing large multi-file work — explicitly requests plans up front ("Please Plan And Impliment Everything", "Develop A Plan To Implement Gift Sending And Recieveing Also The Chating") and accepts the recommended options verbatim, confirming the plan is presented for approval before code is written. Confidence: 0.95
- Approves adding a well-justified new dependency (e.g., framer-motion for animations) to the existing stack when the design spec calls for it, rather than requiring zero-dependency CSS fallbacks. Confidence: 0.55
- Prefers a premium, glassmorphic, animated visual language for UI (rounded corners, blur effects, transparent/glass cards, gradient buttons, floating shadows, soft glow, micro-interactions, spring/scale/fade animations, full-screen immersive layouts), inspired by leading consumer apps like BIGO LIVE — the premium roulette spec explicitly cited Evolution Casino, Stake Originals, Roobet, Bigo Live Mini Games, and Apple Liquid Glass with glassmorphism/blur/glow/gradient borders. Confidence: 0.65
- Expects features that don't exist in the current backend to be delivered as graceful UI shells that render only when relevant data exists (e.g., PK battle / multi-guest seats hidden until remoteUsers > 1) — additive, zero backend changes. Confidence: 0.5
- Runs a long-lived dev server during development and may not restart it after config/CSS changes — when the user pastes a dev-only error (e.g., a Tailwind `@apply` class "does not exist") that doesn't reproduce in a fresh production build, the expected resolution is to verify against a fresh build/dev boot, diagnose the stale-server/PostCSS-cache cause, and tell the user to fully restart the dev server (and clear `node_modules/.vite` / use `npx vite --force` if needed), not to change working code. Confidence: 0.5
- Expects user-selected modes/configuration (e.g., the stream type picked at creation) to drive behavior end-to-end across the stack — not just be stored as a DB field — so a "voice" room captures/publishes only mic (no camera permission request, no camera UI/controls, voice placeholder visual) while video/game rooms keep camera+mic; called out the mismatch ("If It Is Not A Voice Room Then Why Are You Using Both Side Audio Video Capturing?") and confirmed the exact media mapping via clarifying options before implementation. Confidence: 0.65
- Expects user-selected modes/configuration (e.g., the stream type chosen at creation) to drive behavior end-to-end across the stack — not just be stored as a DB field — so a "voice" room captures/publishes only mic (no camera permission prompt, no camera UI/controls, voice placeholder visual) while video/game rooms keep camera+mic; called out the mismatch ("If It Is Not A Voice Room Then Why Are You Using Both Side Audio Video Capturing?") and confirmed the exact media mapping via clarifying options before implementation. Confidence: 0.65
- Prefers a strict host-only broadcast model for go-live streams — only the host shares audio/video; viewers are pure audience and must never be treated as guests (no guest windows, guest labels, or multi-seat layouts, no publish path), with the host's video as the single full-screen focal point for everyone; stated tersely ("only host shares audio and video no user is to be joined as guest they are just viewers"). Confidence: 0.7
- Expects core transactional flows (e.g., virtual-currency gift sending) to be hardened to "100% working" reliability — when the user says a flow "is not ok, make it 100% working," the fix should center on failure-proofing: atomic DB updates to prevent double-spend/race conditions (backend), in-flight guards, disabled/loading states, and inline error feedback (frontend) — while leaving business logic (cuts, transaction shapes, notifications) untouched; when offered "harden the flow" vs "harden + richer UX," chose the recommended hardening-only scope. Confidence: 0.7
- Expects eligibility gates for a feature to depend only on the actor's own capability, not on any unrelated third-party state — gift sending should be gated solely on the sender's balance, not on whether the host record resolves; the UI must never disable a valid action because of an unrelated data-resolution quirk ("any one can send gift if they have balance"). Confidence: 0.6
- Prefers consolidating all infrastructure/services under a single provider/account (asked "I want everything on firebase is that possible?") rather than a patchwork of separate vendors (Render + MongoDB Atlas + Firebase + Agora + Cloudinary) — values one platform for hosting, backend, and database, even if the realistic solution is a pragmatic hybrid under the same Google/Firebase account (Firebase Hosting + Cloud Run + Atlas) instead of a full rewrite. Confidence: 0.6
- Expects reward/earning systems to be strictly gated on verifiable user activity — e.g., a daily live-stream reward is claimable only if the user actually streamed the required number of hours that day, and the system must verify the requirement server-side so users can't claim without earning it ("ঘন্টা করে লাইভ করতেই হবে না হলে রিওয়ার্ড ক্লেইম করতে পারবে না ইউজার"). Confidence: 0.6
- Expects new games/features to be implemented by mirroring the established in-repo architecture end-to-end rather than inventing new structure — approved a plan that explicitly stated Roulette would copy the Aviator engine shape (module state + timers), TeenPatti patterns for socket handlers, models, routes, atomic balance deduct, `game_bet`/`game_win` transactions, `balance:update` pushes, and the frontend hook/context/components/page structure, wired across backend registration points, the app-user GameHub/route, and admin dashboard stats; reinforced when the user asked to rebuild Aviator from scratch "To All Corrected Version" mirroring the Roulette/TeenPatti pattern exactly; the roulette rebuild again kept socket event names, REST route shapes, and repo conventions (game self-contained under src/game/<game>/) untouched. Confidence: 0.85
- Prefers a complete teardown and from-scratch rebuild when an existing feature is fundamentally broken, rather than continuing to patch it — asked to "Remove The Aviator Game Totally, Then Create The Game From Scratch To All Corrected Version" the day after a targeted fix to the crash-point generator, with the rebuild mirroring the repo's corrected reference pattern. Confidence: 0.55
- Expects a removed/rebuilt feature to leave no dead code behind — the entire legacy footprint (standalone package, duplicate root app with the Unity build, old backend engine files, old frontend UI/hook/types) plus now-unused dependencies (react-unity-webgl), stale API files (game.api.ts), and stale comments referencing deleted modules should all be deleted, not just the main feature files. Confidence: 0.5
- Prefers lightweight pure-HTML5 implementations over heavy embedded third-party runtimes for game visuals — chose the HTML5 plane animation over keeping the Unity WebGL AirCrash build for the rebuilt Aviator game (no multi-MB .wasm download, consistent with the rest of the app). Confidence: 0.5
- Prefers numeric form inputs (bet amount, cash-out target, etc.) to preserve full text-editability — the value should be kept as string state while typing so blank/decimal/deletion all work without being force-clamped on every keystroke, then parsed, validated, and clamped only at submit (min/max bounds applied at submission, not during input) — this pattern was applied as the fix for the Aviator BetPanel inputs so users could actually edit them. Confidence: 0.6
- Delivers work items as numbered backlog entries with explicit severity/priority tags and terse one-line descriptions — e.g., "7. Live Critical Bug - Crash HIGH Priority / Live এ SMS করলেই App Crash করে", "8. Group Video Call Feature / Join Option + Max 10 Members Limit", "10. Live এ Host Profile Bug — Live থেকে Host এর Profile এ ঢোকা যায় না", "11. Setting এ Password Change Option — Setting > Account এ Change Password Add করতে হবে", and "13. 1:1 Random Match Call Option — Match Button > Random 1:1 Video/Audio Call > Next + Stop Button", and "14. Official Notification - Mic Icon — App + Website উপরে Right Corner এ Mic Icon। সব Official Notification এখানে আসবে + Red Dot" — mixing Bengali with English, each item carrying exactly the essential constraint(s) and leaving investigation, design, and implementation to the assistant. Confidence: 0.75
- Prefers call-system semantics where a non-initiator hanging up only removes them (others stay in the call) and the call ends only when the last remaining member leaves or the initiator hangs up ("stay until last person" over ending the call for everyone when one member leaves); also favors entry points on multiple surfaces (both Discover and Chats) when given the option. Confidence: 0.6
- Expects live/active indicators to be truthful and self-correcting — content that is no longer live (stream ended / host offline) must drop out of list UIs automatically (periodic refetch, client-side stale-heartbeat filtering, server-side liveness in feed queries) and never keep an unconditional LIVE badge; reported "Live Off হলেও দেখায়" (shows even when off) as a critical bug. Confidence: 0.6
- Expects viewer/participant counters to be accurate and never negative — idempotent atomic join/leave (per-viewer dedupe), decrement on socket disconnect, schema `min: 0`, and `Math.max(0, …)` clamps at both feed and display layers; reported negative viewer counts ("Viewer Count Minus") as a critical bug. Confidence: 0.55
- Expects stream/media list cards to always render a real thumbnail — derived server-side (cover → host avatar) so cards never appear as a bare gradient/icon placeholder; reported missing video thumbnails on cards ("Video নাই") as a critical bug. Confidence: 0.5
- Expects sensitive identity data (NID/OLAID numbers, identity document images, selfies) to be handled with explicit data-minimization and strict access control — store only what's needed, keep review access admin-only, never expose identity documents as public image URLs (use private/restricted delivery for document uploads), and enforce strong production security rules; flagged as an "Important" constraint in the verification spec. Confidence: 0.7
- Expects feature restrictions to be enforced authoritatively at the backend/API level — middleware that re-queries the DB (so stale JWTs can't bypass), never just hiding UI buttons; security-critical rules must live in backend-authoritative configuration rather than frontend settings ("শুধু UI button hide করলে হবে না", "backend-এর authoritative configuration থাকতে হবে"). Confidence: 0.75
- Prefers a central permission system / permission matrix so feature gating is defined once and shared across endpoints (e.g., a single requireVerification middleware covering Go Live, Voice Party, and agency features) for easier future development ("central permission system রাখলে future development অনেক সহজ হবে"). Confidence: 0.6
- Expects gating to restrict only the specific capability that requires it rather than locking the whole account — e.g., an unverified host is blocked from Go Live/Voice hosting but still allowed to watch live, play games, and send gifts; explicitly called this account-not-locked rule the most important in the verification spec ("Unverified Host-কে পুরো account lock করবেন না"). Confidence: 0.6
- Verifies finished work on the live deployed site and reports gaps by pointing at the deployment URL — e.g., a terse message linking the deployed Settings page ("There Is No Agency Mount Unmount Process Here https://…/settings") when a newly added feature wasn't showing, and again reporting "https://nevo-live-app-user.onrender.com/recharge is showing static agent, the linked agent will be automatically selected" right after the auto-assign removal was committed and pushed; the live build is expected to reflect the latest work, so when the deployed build is behind the working tree (uncommitted changes), the resolution is to commit and push so the auto-deploy pipeline (Render building from origin/main) picks it up — local working-tree code alone is not considered "done" until it's live; again reported a problem by pasting only the live URL ("https://nevo-live-app-user.onrender.com/recharge problem is here") with no description, leaving the assistant to verify the deployed bundle against the fix, diagnose the behavioral gap, and push a corrected version, then confirm the new bundle hash went live; and again reported a discrepancy by citing two live URLs side by side ("https://…/withdraw shows correct agency but https://…/recharge shows wrong agency") as the entire bug report. Confidence: 0.9
- Expects pages within the same app that surface the same underlying entity to display identical, correct data — a page showing a different value than a sibling page for the same thing (e.g., the user's linked agency shown correctly on Withdraw but wrongly on Recharge) is reported as a bug in a single line citing both live URLs; the resolution must make every page resolve from the same backend source of truth (e.g., merging the linked agent into the recharge payable list so the picker can never show a different "wrong" agent than the linked one) rather than leaving per-page logic that can disagree. Confidence: 0.6
- Expects money-flow (deposit/recharge) agent selection to default to the user's linked agent — the linked agent is pre-selected automatically on the recharge page (with a hint "Your linked agent is pre-selected — you can change it below") yet remains changeable to any other agent; reported tersely as "Agent is auto stucked, this must be depended on the users linked agent" and confirmed by choosing "Default to linked, still changeable" over a hard-locked auto-use option — this reverses the earlier "Stop Auto Assigning Agents" stance (the frontend must not show a free picker disconnected from the user's linked agent). The backend must still require an explicit agentId and money flows still fail closed (blocked client-side + server-side error) when no agent is present; also expects the pre-selection to track the link over time — relinking to another agent must make the page show the newly linked agent, so a page that keeps displaying the old agent after a relink (reported tersely as "If I Change The Agent, Relink With Another Agent It Shows The Old Agent On This Page <live recharge URL>") is a bug, and the link must be re-resolved when it changes rather than only fetched once on mount with a stale-guard; likewise, when the user is unlinked, the stale auto-selected agent must be cleared entirely so an unlinked user never sees a leftover linked agent (reported tersely as "If Unlinked To Any Agent Still Shows UID: 168003 as agent") — the pre-selection must mirror the backend's current link state including the unlinked/none case, and a guard that preserves a "manual pick" must not block that clear on a fresh mount after an unlink. Confidence: 0.85
- Expects the backend to be treated as the single source of truth for user relationship/state — client logic that drives defaults/pre-selection (e.g., linking recharge to the user's linked agent) must not gate on the persisted client user store (stale/absent `user.agencyId`, e.g. when an admin provisioned the link), but always resolve the current state from the backend endpoint (`/agency/my-agency`) and pre-select from that response; validated this diagnosis by inspecting the auth store, User model serialization, and live API data rather than accepting the first fix. Confidence: 0.7
